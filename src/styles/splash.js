import { StyleSheet } from "react-native";

const style = StyleSheet.create({
    container: {
        flex: 1,
    },

    center: {
        width: "100%",
        height: "100%",
        paddingVertical: 48,
        justifyContent: "flex-end",
        alignItems: "center",
    },

    txt: {
        padding: 18,
        backgroundColor: "#3f3f46a3",
        color: "#fff",
        borderRadius: 18,
    }
});

export default style;